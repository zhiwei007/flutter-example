import 'dart:io';

void main() {
  /*HttpServer.bind(InternetAddress.anyIPv4, 9999).then((server) {
    server.listen((HttpRequest request) {
      print(request.response.toString());
      request.response.write('Hello, dart!');
      request.response.close();
    });
  });*/
  print(24000*14);
  print(2.05*15+3);
}
